% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npsa_utils.R
\name{npsa_times}
\alias{npsa_times}
\alias{summary.npsa_times}
\alias{plot.npsa_times}
\title{Check default analysis times}
\usage{
npsa_times(
  time,
  event,
  fit.times = NULL,
  nuisance.options = list(),
  plot.times = NULL,
  rv.times = NULL,
  rmst = FALSE,
  fit.times.rmst = NULL,
  max.fit.times = 50,
  max.eval.times = 200,
  G.cutoff = 0.05,
  verbose = FALSE
)

\method{summary}{npsa_times}(object, digits = 4, ...)

\method{plot}{npsa_times}(x, ...)
}
\arguments{
\item{time}{Numeric vector of event or censoring times.}

\item{event}{Numeric vector of event indicators (1 = event, 0 = censored).}

\item{fit.times}{Optional user-provided analysis times.}

\item{nuisance.options}{Optional nuisance options. If
\code{nuisance.options$eval.times} is supplied, it is preserved.}

\item{plot.times}{Optional reporting times. If \code{NULL}, all fitted times
are used.}

\item{rv.times}{Optional RV/MIRV times. If \code{NULL}, \code{plot.times}
are used when supplied; otherwise about five representative fitted times
are used.}

\item{rmst}{Logical; if TRUE, preview RMST horizon settings.}

\item{fit.times.rmst}{Optional RMST horizon times. If \code{NULL} and
\code{rmst = TRUE}, the preview uses \code{max(fit.times)} and reminds
users that RMST horizons need analyst interpretation.}

\item{max.fit.times}{Maximum number of automatic fitted times.}

\item{max.eval.times}{Maximum number of automatic nuisance prediction times.}

\item{G.cutoff}{Practical reverse-KM censoring-support cutoff for automatic
fitted times.}

\item{verbose}{Logical; if TRUE, print a short message when automatic times
are chosen.}

\item{object}{An object returned by \code{\link{npsa_times}()}.}

\item{digits}{Number of digits for printing.}

\item{...}{Additional arguments.}

\item{x}{An object returned by \code{\link{npsa_times}()}.}
}
\value{
A list of class \code{npsa_times} containing \code{fit.times},
\code{eval.times}, \code{plot.times}, \code{rv.times},
\code{fit.times.rmst}, \code{time.info}, and \code{censor.df}.

For \code{summary()}, invisibly returns \code{object}. For \code{plot()},
returns a \code{ggplot} object.
}
\description{
This helper shows the default time grid that \code{\link{np_surv}()} and
\code{\link{npsa_surv}()} would use when \code{fit.times} is not provided.
It does not fit nuisance models or estimate treatment effects.
}
\examples{
\dontrun{
time.out <- npsa_times(time, event)
summary(time.out)
plot(time.out)

time.out <- npsa_times(time, event,
                       fit.times = seq(0.1, 1.2, by = 0.1),
                       plot.times = c(0.2, 0.6, 1.0),
                       rv.times = c(0.2, 0.6, 1.0))
summary(time.out)

time.out <- npsa_times(time, event,
                       fit.times = c(0.2, 0.4, 0.6, 0.8))
summary(time.out)
}
}
